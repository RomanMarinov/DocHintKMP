import SwiftUI

/// Общие метрики для заметных кнопок (`borderedProminent`): FAB «Отправить», «Распознать» и т.д.
enum AppProminentActionMetrics {
    static let labelHorizontalPadding: CGFloat = 16
    static let labelVerticalPadding: CGFloat = 8
    static let labelFont: Font = .subheadline
}
